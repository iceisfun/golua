# Lua 5.5 Language Features

Demonstrates Lua 5.5 features: named vararg parameters, prefix attribute syntax (`local<const>`, `local<close>`), `table.create`, read-only for-loop variables, and `error(nil)` behavior.

## Run

```bash
go run ./examples/lua55_features
```
