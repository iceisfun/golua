# check example

Demonstrates the `check` package: parses incomplete Lua source and prints
the partial AST alongside JSON diagnostics suitable for Monaco editor.

```bash
go run ./examples/check/
```
